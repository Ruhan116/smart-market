# Ignore BMAD and Claude assistant helper folders only
/.bmad-core/
/.claude/
# Environment files (local overrides)
.env
.env.local
.env.example
# Large ML datasets and models - use Git LFS or cloud storage instead
ml_models/datasets/
ml_models/training/
